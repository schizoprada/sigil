[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sigil"
dynamic = ["version"]
description = "runtime & execution suite"
dependencies = [
    "loguru",
    #"typical @ git+https://github.com/schizoprada/typical.git"
]

[tool.hatch.version]
path = "src/sigil/__init__.py"
