[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sigil"
dynamic = ["version"]
description = "runtime & execution suite"
dependencies = [
    "loguru",
    "typical"
]

[tool.hatch.version]
path = "src/sigil/__init__.py"
